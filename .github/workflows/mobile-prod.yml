name: Native Mobile Tests - PROD

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Mobile Platform'
        type: choice
        required: true
        options:
          - android
          - ios
        default: 'android'
      component:
        description: 'Test file to run on PROD'
        type: choice
        required: true
        options:
          - all (runs all read-only tests)
          - com.company.automation.tests.LoginTests
        default: 'all (runs all read-only tests)'

jobs:
  test:
    timeout-minutes: 60
    runs-on: ${{ github.event.inputs.platform == 'ios' && 'macos-14' || 'ubuntu-latest' }}
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Enable KVM Hardware Acceleration
        if: ${{ github.event.inputs.platform == 'android' }}
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm || true

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Appium
        run: |
          npm install -g appium
          appium -v

      # Android Setup
      - name: Setup Android Driver
        if: ${{ github.event.inputs.platform == 'android' }}
        run: |
          appium driver install uiautomator2

      - name: Ensure Android PROD APK Available
        if: ${{ github.event.inputs.platform == 'android' }}
        run: |
          mkdir -p apps/prod
          if ! ls apps/prod/*.apk apps/*.apk 1> /dev/null 2>&1; then
            echo "No PROD APK found in apps/prod/. Place seedling-prod.apk in apps/prod/ or apps/."
          else
            echo "Found PROD APK file in apps folder."
          fi

      # iOS Setup
      - name: Setup iOS Driver & Simulator
        if: ${{ github.event.inputs.platform == 'ios' }}
        run: |
          appium driver install xcuitest
          
          # Unpack iOS App if zipped
          for zipfile in apps/prod/*.zip apps/*.zip; do
            if [ -f "$zipfile" ]; then
              echo "Unpacking $zipfile into apps/prod/..."
              mkdir -p apps/prod
              unzip -o "$zipfile" -d apps/prod/
            fi
          done

          # Boot iPhone 15 Simulator
          UDID=$(xcrun simctl list devices available | grep "iPhone 15" | head -1 | grep -oE "\([0-9A-F-]+\)" | tr -d "()")
          if [ -z "$UDID" ]; then
            UDID=$(xcrun simctl list devices available | grep "iPhone" | head -1 | grep -oE "\([0-9A-F-]+\)" | tr -d "()")
          fi
          echo "Booting iOS Simulator UDID: $UDID"
          xcrun simctl boot $UDID || true
          xcrun simctl bootstatus $UDID -b || true

      - name: Start Appium Server in Background
        run: |
          nohup appium --log appium.log --port 4723 > /dev/null 2>&1 &
          sleep 5
          curl --retry 5 --retry-delay 2 --retry-connrefused http://127.0.0.1:4723/status || echo "Appium warming up..."

      - name: Determine Test Command
        id: set-command
        run: |
          PLATFORM="${{ github.event.inputs.platform }}"
          COMP="${{ github.event.inputs.component }}"

          if [[ "$COMP" == *"all"* ]]; then
            CMD="mvn test -Denv=prod -Dplatform=$PLATFORM -Dexecution=ci -DsuiteXmlFile=testng-prod.xml"
          else
            CMD="mvn test -Denv=prod -Dplatform=$PLATFORM -Dexecution=ci -Dtest=$COMP"
          fi

          echo "TEST_COMMAND=$CMD" >> $GITHUB_ENV
          echo "Selected Command: $CMD"

      # Execute Tests (Android uses reactivecircus emulator, iOS runs directly on simulator)
      - name: Run Android Tests on Emulator
        if: ${{ github.event.inputs.platform == 'android' }}
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 31
          target: default
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: |
            ${{ env.TEST_COMMAND }} 2>&1 | tee test-output.log
            exit ${PIPESTATUS[0]}


      - name: Run iOS Tests on Simulator
        if: ${{ github.event.inputs.platform == 'ios' }}
        run: |
          ${{ env.TEST_COMMAND }} 2>&1 | tee test-output.log
          exit ${PIPESTATUS[0]}

      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: mobile-report-prod-${{ github.event.inputs.platform }}
          path: |
            target/index.html
            target/surefire-reports/
            logs/
            appium.log
          retention-days: 30

      - name: Deploy Report to Surge
        if: ${{ !cancelled() && env.SURGE_LOGIN != '' }}
        env:
          SURGE_LOGIN: ${{ secrets.SURGE_LOGIN }}
          SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
        run: |
          if [ -f target/index.html ]; then
            npx surge ./target ${{ github.run_id }}-seedling-mobile-prod.surge.sh
          fi

      - name: Prepare Email Body
        if: ${{ !cancelled() }}
        run: |
          REPORT_URL="https://${{ github.run_id }}-seedling-mobile-prod.surge.sh"

          echo "Mobile App Automation test run finished." > email-body.txt
          echo "Status: ${{ job.status }}" >> email-body.txt
          echo "Environment: PROD (Read-Only)" >> email-body.txt
          echo "Platform: ${{ github.event.inputs.platform }}" >> email-body.txt
          echo "Component: ${{ github.event.inputs.component }}" >> email-body.txt
          echo "Branch: ${{ github.ref_name }}" >> email-body.txt
          echo "Run link: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> email-body.txt
          echo "" >> email-body.txt

          if [ "${{ job.status }}" != "success" ]; then
            echo "=== TEST FAILURES ===" >> email-body.txt
            if [ -f test-output.log ]; then
              grep -E "FAILURE|ERROR|Exception|AssertionError" test-output.log | head -20 >> email-body.txt || echo "See ExtentReport for failure screenshot." >> email-body.txt
            fi
            echo "=====================" >> email-body.txt
            echo "" >> email-body.txt
          fi

          echo "VIEW INTERACTIVE HTML REPORT HERE (One-Click):" >> email-body.txt
          echo "$REPORT_URL" >> email-body.txt
          echo "" >> email-body.txt

      - name: Send Email Report
        if: ${{ !cancelled() && env.EMAIL_USERNAME != '' }}
        uses: dawidd6/action-send-mail@v3
        env:
          EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Mobile PROD [${{ github.event.inputs.platform }}] Run ${{ job.status }} - ${{ github.event.inputs.component }}"
          to: ${{ secrets.TL_EMAIL }}
          from: Seedling Automation <${{ secrets.EMAIL_USERNAME }}>
          body: file://email-body.txt
