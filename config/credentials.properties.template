# Copy this file to credentials.properties (same folder) and fill in real values.
# credentials.properties is gitignored - never commit real credentials.
# No prod credentials needed here since prod tests are read-only / no login.

qa.username=REPLACE_WITH_QA_TEST_USERNAME
qa.password=REPLACE_WITH_QA_TEST_PASSWORD
